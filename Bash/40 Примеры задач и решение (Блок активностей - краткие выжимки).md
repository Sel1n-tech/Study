# Основы написания
- Вместо того чтобы вводить команды по одной, вы можете объединить их в одну строку, разделяя точкой с запятой `;`
- Создание файла - Создать файл через touch или nano/vim/vi - далее добавляем #!/bin/bash. Добавьте нужные команды, каждую с новой строки:

![](https://ucarecdn.stepik.net/3fd01bf3-7091-4443-aa11-cecea74a87a1/)

Ctrl+O - записать в файл. Ctrl+X - выйти из редактора. Комментарии (строки, начинающиеся с `#`) игнорируются при выполнении. 
- Просто так файл не запустится, пока не выдашь ей права, по умолчанию, при создании файла, у него есть права только на чтение и запись (`rw-r--r--`). Дайте себе право на выполнение с помощью команды `chmod` : chmod u+x (имя файла)
- Запускаем ./(имя файла)

## Простой вывод
- echo "Это тест"
## Работа с кавычками

Будьте осторожны с кавычками внутри строки. Используйте разные типы кавычек, чтобы избежать ошибок.

**Правильно:**

```bash
echo "Это тест, чтобы проверить, внимательны ли вы"
echo 'А вот текст в "кавычках".'
```
Если внутри строки есть апостроф `'`, используйте двойные кавычки `"` и наоборот.


# Работа с переменными в Bash-скриптах
## Системные переменные (окружение)
Чтобы увидеть все доступные переменные окружения, используйте команду:

```bash
set
```

![697](https://ucarecdn.stepik.net/99eb2626-1c91-4d25-ab0d-7f0158416733/)

Часто используемые переменные — `HOME`, `USER`, `PATH`. Запомните их — они вам пригодятся!

![](https://ucarecdn.stepik.net/2cebe9b2-4d4a-43de-a8e5-365f345ddc75/)

Чтобы получить значение переменной, нужно поставить перед её именем знак доллара `$`.

#### Пример скрипта `user_info.sh`:

```bash
#!/bin/bash
# Выводим информацию о текущем пользователе
echo "Привет, $USER!"
echo "Твой UID (идентификатор пользователя): $UID"
echo "Твоя домашняя директория: $HOME"
Переменные чувствительны к регистру! `$USER` и `$user` — это разные переменные (если `$user` не был создан, он будет пустым).
```

## Как избежать ошибок с символом `$`
Иногда вам нужно вывести текст, содержащий символ `$`, например, цену товара.

Shell интерпретирует `$15` как переменную с именем `1`, которая не существует, поэтому выводит пустую строку. Оставшийся `5` — это просто текст.

**Правильно:**

```bash
echo "Стоимость товара: \$15"
```

![](https://ucarecdn.stepik.net/6b09e466-331e-4cc6-aacd-2502f3221a6d/)

_Как работает?_ Обратный слэш `\` экранирует символ `$`, заставляя shell воспринимать его как обычный текст.

**Альтернатива:** можно использовать одинарные кавычки `'...'`, внутри которых переменные не подставляются:

## Создание своих переменных (пользовательские переменные)
### Как объявить переменную?

Синтаксис очень простой:

```bash
имя_переменной=значение
```

**Важные правила:**

- Никаких пробелов до и после знака `=`.
- Имя может содержать буквы, цифры и символ `_` (нижнее подчеркивание).
- Имя чувствительно к регистру: `MyVar` ≠ `myvar`.
- Длина имени — до 20 символов (хотя на практике ограничений нет).
- 
**Примеры:**

```bash
count=5
name="Тукс"
message="Привет, мир!"
is_active=true

#!/bin/bash 
name="Тукс"
message="Привет, $name!"
echo $message 

выводит: Привет, Тукс!
```

## Командная подстановка — сохраняем результат команды в переменную

### Способ 1: Обратные кавычки (backticks) `` `...` ``

```bash
current_date=`date`
echo "Сегодня: $current_date"
```

![](https://ucarecdn.stepik.net/04e3cb3d-5f8a-4d6c-8a0e-6e60f1420248/)

### Способ 2: `$()` — предпочтительный способ

```bash
current_date=$(date)
echo "Сегодня: $current_date"
```

![](https://ucarecdn.stepik.net/46e843f8-e140-4119-86b3-1e97bd5a40b8/)

Используйте `$()`, так как это современный стандарт, более читаемый и позволяет вкладывать команды друг в друга.

### Пример скрипта `log_today.sh`:

```bash
#!/bin/bash
# Создаём лог-файл с текущей датой в имени

# Получаем дату в формате ГГММДД
today=$(date +%Y%m%d)

# Создаём файл с логом
ls -l /usr/bin > log.$today

echo "Лог-файл создан: log.$today"
echo "Содержимое файла:"
cat log.$today
```


## Перенаправление ввода или вывода и конвейеры в Bash

### Основной оператор: `>`

Оператор `>` перенаправляет вывод команды в файл. Если файла нет — он создаётся. Если файл существует — его содержимое **перезаписывается**.

**Пример:**

```bash
date > current_time.txt
cat current_time.txt
```

![](https://ucarecdn.stepik.net/8e7c5e06-a391-41b1-83f7-3eba882c03ad/)

Используйте `>` для создания нового файла или полной перезаписи существующего.

### Оператор добавления: `>>`

Если вы хотите **добавить** новый вывод в конец файла, а не стирать старый, используйте `>>`.

**Пример:**

```bash
echo "Начало работы" >> log.txt
date >> log.txt
echo "Завершение работы" >> log.txt
cat log.txt
```

![](https://ucarecdn.stepik.net/8bf941d6-b524-4de0-9f95-0c686997c2fc/)

**Идеальный вариант для логов!** Каждый раз, когда вы запускаете скрипт, новая запись будет добавляться в конец файла.




