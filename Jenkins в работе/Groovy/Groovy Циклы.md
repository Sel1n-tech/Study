## Цикл for

Цикл **for** действует следующим образом.

![](https://lh3.googleusercontent.com/0VfANE40Ev2p3G-1lsccsgziIHDOZRuArJYfbM2d3HnvFQReemV448Q62wJkXe7fLWZuTfbZXAXiEOqbrIoQWwmL8BEMmdctc5J0YF-H6M4W7lzTo1if843-sLvzsiOdNgvP15dZJ3y7XPofJsC4ZA)

Когда цикл начинается, выполняется его инициализация.

В общем случае это выражение, устанавливающее значение переменной управления циклом, которая действует в качестве счетчика, управляю­щего циклом. Важно понимать, что первая часть цикла for, содержащая инициа­лизирующее выражение, выполняется только один раз. 

Затем вычисляется задан­ное условие, которое должно быть логическим выражением.

Как правило, в этом выражении значение управляющей переменной сравнивается с целевым значени­ем.

Если результат этого сравнения истинный, то выполняется тело цикла. А если он ложный, то цикл завершается. И наконец, выполняется третья часть цикла

**for** - итерация. Обычно эта часть цикла содержит выражение, в котором увеличивается или уменьшается значение переменной управления циклом. 

Затем цикл повторяется, и на каждом его шаге сначала вычисляется условное выражение, по­ том выполняется тело цикла, а после этого вычисляется итерационное выражение.

Этот процесс повторяется до тех пор, пока результат вычисления итерационного выражения не станет ложным.

![297](https://lh4.googleusercontent.com/CKMspFkQvZCnLrIYEttzZUJM0sgKqVlqKvM2zTjHljFBq1W-s5Ct0728W977yglKR4RmtCpxItZK9UzptU4-DjIFc3wKR3RQ-4TspW9xrhcDvkchPVQFxlAahnlb7mE6VlZ61RrbtEc8n7ZgSw-3Ig)

Пример 1. 

```java
String msg = ''
for (int i = 0; i < 4; i++) {
    msg += 'Hi '
}
println msg
```

# for - in

В Groovy языке программирования цикл for-in используется для итерирования по элементам коллекции или любому другому итерируемому объекту. Он работает аналогично оператору foreach в других языках, таких как JavaScript или Python.

Вот пример использования цикла for-in в Groovy:

```nginx
def numbers = [1, 2, 3, 4, 5]

for (number in numbers) {
   println "current number: $number"
}
```

В этом примере мы создаем массив чисел и проходим по каждому числу с помощью цикла for-in. На каждой итерации мы выводим текущее число на экран.

![286](https://ucarecdn.stepik.net/d6713c8c-0ebf-4ec0-9a96-0fdcfb1be714/)

Обратите внимание, что цикл for-in не ограничивается только итерацией по коллекциям. Он также может использоваться для итерирования по любому итерируемому объекту, например, массиву или строке.

Оператор **for-in** также может использоваться для перебора диапазонов.

Пример 2. (В этом коде мы перебираем элементы от 0 до 9 и выводим их на экран.)

```java
for ( i in 0..9 ) {
    print i + ' | '
}
```

![277](https://ucarecdn.stepik.net/41c019ce-b004-45f0-9afe-0a031e99f865/)

Пример 3.(Перебираем символы строки)

```java
def text = "QWERTY"
def list = []
for (ch in text) {
    list.add(ch)
}
println list
```

![159](https://ucarecdn.stepik.net/bbac3fdd-4b71-41ec-8946-705a04bf4173/)

## Цикл while

**while** является самым простым для организации циклов

Он повторяет оператор или блок операторов до тех пор, пока значение его управляющего выражения истинно.

Этот оператор цикла имеет следующую об­щую форму

![](https://lh4.googleusercontent.com/EZ0bGgj0P_IR6NlodgZEmsOAzGv9IviFkb5j1GwaFiZe4zYwn1q7IPxT-pVrNS11smF95wX8hH6uVY_av6TlDFfk6ZcCfDlY3PlFDuvHNEvtamu5W3Oo31Hb26YNs-WKisBdUFU94NCYdDclffnFTg)

![](https://lh4.googleusercontent.com/NeYqakfS_L6lc_wvTflkS-O4sEhJEkUEX7QSScZqc-Bv0Qv594aeXyOVcS-pa9kqG9Zte27FkX62gdSMzEBmEHkfAYuxz2XAPcPZEMnZ0Uxfi4LuA3qFEwZ5RkVLv75r0TnHueNya0pFbneR4C63AQ)

Здесь условие обозначает любое логическое выражение. 

Тело цикла будет вы­полняться до тех пор, пока условное выражение истинно. 

Когда условие стано­вится ложным, управление передается строке кода, непосредственно следующей за циклом.

Фигурные скобки могут быть опущены только в том случае, если в цик­ле повторяется лишь один оператор.

Пример 1.

```applescript
def count = 5
while( count < 21 ) {
    print count + ' | '
    count += 3
}
```
![](https://ucarecdn.stepik.net/b1036d09-6cf8-447b-ad21-2860059a8833/)

## Цикл do - while

Иногда тело цикла желательно выполнить хотя бы один раз, даже если в начальный момент условное выражение ложно.

Возможны случаи, когда про­верку условия прерывания цикла желательно выполнять в конце цикла, а не в на­чале.

Для этой цели в Groovy предоставляется цикл, который называется **do-while**

Тело этого цикла всегда выполняется хотя бы один раз, поскольку его условное вы­ражение проверяется в конце цикла.

Общая форма цикла do-while следующая:

![](https://lh4.googleusercontent.com/qoGVStnwqikF6QE6Oy5sLFHXUh8RIUxpakcfPXjf6ahX93_eL666Dl-nKwr-9ANrK5_4kWvSFYsO1XF4Um6HWI4x3kWxvghRMPTyzxtT1S1dk2eT8P3tpexKETqYGOQQU1M1xJfEVowKqTgX6nFRCg)

При каждом повторении цикла **do-while** сначала выполняется тело цикла, а за­тем вычисляется условное выражение.

Если это выражение истинно, цикл повторя­ется. В противном случае выполнение цикла прерывается. Как и во всех остальных циклах в Java, заданное условие должно быть логическим выражением.

![](https://lh4.googleusercontent.com/M54hEnc366Jvn3IBYE3yxQVL4OfpoBJO-0JIS9oFIt316g7NNdQuFfyuOmyGWjmWM0WCP7cuDX4bMTDoXaWKdC3RQ1mgmXTQAEFTUex9fD2bbQ3bjyoOVKDf6Gep6EUfQn2vlz-SpOfwJWO45IzUOA)

Пример 1. 

```java
def count = 5
def fact = 1
do {
    fact *= count--
} while(count > 1)
println fact // fact: 120
```


# Операторы перехода

## Оператор break

Оператор **break** используется для изменения выполнения циклов и оператора switch.

```java
for(int i in [0, 10, 200, 30, 45, 500]) {
  print(i + ' | ');
  if(i == 30){
    break
  }
}
```

если **i == 30** то последнее значение массива, 45 и 500 не выводится, так как цикл прерывается break.

## Оператор continue

При выполнении **continue** - управление передаётся к условию проверки в начале цикла, для того что бы определить может ли цикл продолжать своё выполнение, а все операторы в цикле, находящиеся после **continue** игнорируются. 

В следующий пример демонстрирует нам пропуск одного значения 30, и остальное выводит на печать.

Пример 1.

```java
def numbers = [0, 10, 200, 30, 45, 500]
for(int i = 0; i < numbers.size(); i++ ) {
  if(numbers[i] == 30)
    continue
  print(numbers[i] + ' | ')
}
```

![](https://ucarecdn.stepik.net/75534298-464d-4902-b991-b76cc07355d8/)